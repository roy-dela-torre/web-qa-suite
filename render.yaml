services:
  - type: web
    name: web-qa-suite-backend
    runtime: docker
    dockerfilePath: server/Dockerfile
    dockerContext: server
    plan: free
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
